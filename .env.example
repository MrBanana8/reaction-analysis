# Supabase (optional - for database storage)
# SUPABASE_URL=your_supabase_url
# SUPABASE_KEY=your_supabase_anon_key

# WebSocket server
WS_HOST=0.0.0.0
WS_PORT=8765

# Output file for results
OUTPUT_FILE=emotion_results.json

# Performance: analyze every Nth frame (1 = all frames, 5 = every 5th frame)
ANALYZE_EVERY_N_FRAMES=5
