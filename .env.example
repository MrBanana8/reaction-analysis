# Hume AI API key (required)
HUME_API_KEY=your_hume_api_key

# WebSocket server settings
WS_HOST=0.0.0.0
WS_PORT=8765

# Analyze every Nth frame (default: 1 = all frames, 5 = every 5th frame)
ANALYZE_EVERY_N_FRAMES=5
